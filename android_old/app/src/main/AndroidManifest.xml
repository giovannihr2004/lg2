<!-- 📄 Archivo: AndroidManifest.xml -->
<!-- 📍 Ubicación: android/app/src/main/AndroidManifest.xml -->
<!-- 📝 Descripción: Configuración completa con Facebook App ID directo -->
<!-- 📅 Última actualización: 14/05/2025 - 17:46 (Hora de Colombia) -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lector_global">

    <application
        android:name="${applicationName}"
        android:label="Lector Global"
        android:icon="@mipmap/ic_launcher">

        <!-- 🌐 Facebook App ID (valor directo sin strings.xml) -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="1041063751510341"/>

        <!-- 🎯 Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ⚙️ Requerido para plugins de Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 🔍 Permite consultar apps que pueden procesar texto -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
